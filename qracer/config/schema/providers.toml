# .qracer/providers.toml — provider configuration
# Copy this file to ~/.qracer/providers.toml or ./.qracer/providers.toml

[providers.yfinance]
enabled = true
priority = 100
tier = "hot"
kind = "data"

[providers.claude]
enabled = true
priority = 100
kind = "llm"
api_key_env = "ANTHROPIC_API_KEY"

[providers.openai]
enabled = false
priority = 50
kind = "llm"
api_key_env = "OPENAI_API_KEY"

[providers.gemini]
enabled = false
priority = 50
kind = "llm"
api_key_env = "GOOGLE_API_KEY"

[providers.openrouter]
enabled = false
priority = 50
kind = "llm"
api_key_env = "OPENROUTER_API_KEY"
# Model is chosen at `qracer install` time and stored in config.toml (llm_model).
# Browse available models with `qracer models` or `qracer models --search <kw>`.

[providers.finnhub]
enabled = false
priority = 50
tier = "warm"
kind = "data"
api_key_env = "FINNHUB_API_KEY"

[providers.fred]
enabled = false
priority = 50
tier = "warm"
kind = "data"
api_key_env = "FRED_API_KEY"
